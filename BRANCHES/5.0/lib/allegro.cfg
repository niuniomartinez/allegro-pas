(* Common configuration.

  This file is included by library units to configure the compiler.  It isn't
  neccessary to include it in your projects. *)

{TODO: Detect MacOS and iOS}
{TODO: Detect GNU/Pascal}

{$IFNDEF FPC}
  {$IFNDEF DELPHI}
{
  This version wasn't tested with other compilers than Free Pascal and Delphi.
  May be you want to help porting it to other systems and compilers, won't you?
}
    {$ERROR I didn't test Allegro.pas with this compiler!  Do you want to help? }
  {$ENDIF}
{$ENDIF}



{ Detects and configures the compiler. }

{$IFDEF FPC}
{ Free Pascal. }
  {$MODE FPC}
  {$PACKRECORDS C}
  {$LONGSTRINGS ON}
  { $SMARTLINK ON} { Note that it isn't enabled. }
  {$IFDEF UNIX}
    {$IF DEFINED(DARWIN) OR DEFINED(QNX) OR DEFINED(BEOS)}
{
  This version wasn't tested on those systems.
  May be you want to help porting it to other systems and compilers, won't you?
}
      {$ERROR I didn't test Allegro.pas on your Operating System.  Do you want to help? }
    {$ENDIF}
  {$ENDIF}
{$ENDIF}

{$IFDEF DELPHI}
{ Delphi }
  {$PACKRECORDS C}
  {$LONGSTRINGS ON}
{$ENDIF}



{ Defines some constants with the names of the library files. }
CONST
{$IF DEFINED(UNIX)}
  ALLEGRO_LIB_NAME             = 'liballegro.so.5.0';
  ALLEGRO_AUDIO_LIB_NAME       = 'liballegro_audio.so.5.0';
  ALLEGRO_ACODEC_LIB_NAME      = 'liballegro_acodec.so.5.0';
  ALLEGRO_COLOR_LIB_NAME       = 'liballegro_color.so.5.0';
  ALLEGRO_FONT_LIB_NAME        = 'liballegro_font.so.5.0';
  ALLEGRO_IMAGE_LIB_NAME       = 'liballegro_image.so.5.0';
  ALLEGRO_MAIN_LIB_NAME        = 'liballegro_main.so.5.0';
  ALLEGRO_PRIMITIVES_LIB_NAME  = 'liballegro_primitives.so.5.0';
{$ELSEIF DEFINED(WINDOWS)}
{NOTE: It uses monolith staticly linked version. }
  {$WARNING "monoligh staticly linked" version only }
  ALLEGRO_LIB_NAME             = 'allegro-5.0.5-monolith-mt.dll';
  ALLEGRO_AUDIO_LIB_NAME       = 'allegro-5.0.5-monolith-mt.dll';
  ALLEGRO_ACODEC_LIB_NAME      = 'allegro-5.0.5-monolith-mt.dll';
  ALLEGRO_COLOR_LIB_NAME       = 'allegro-5.0.5-monolith-mt.dll';
  ALLEGRO_FONT_LIB_NAME        = 'allegro-5.0.5-monolith-mt.dll';
  ALLEGRO_IMAGE_LIB_NAME       = 'allegro-5.0.5-monolith-mt.dll';
  ALLEGRO_MAIN_LIB_NAME        = 'allegro-5.0.5-monolith-mt.dll';
  ALLEGRO_PRIMITIVES_LIB_NAME  = 'allegro-5.0.5-monolith-mt.dll';
{$ENDIF}
