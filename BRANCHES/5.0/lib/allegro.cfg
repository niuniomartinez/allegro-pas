(* Common compiler configuration.

  This file is included by library units to configure the compiler.  It isn't
  neccessary to include it in your projects. *)

{$IFNDEF FPC}
{
  This version wan't tested with other compilers than Free Pascal.
  May be you want to help porting it to other systems and compilers, won't you?
}
 {$WARNING I didn't test Allegro.pas with this compiler!  Do you want to help? }
{$ENDIF}

{$IFDEF FPC}
{ Free Pascal. }
 {$MODE FPC}
 {$PACKRECORDS C}
 {$LONGSTRINGS ON}
 { $SMARTLINK ON} { Note that it isn't enabled. }
{$ENDIF}
